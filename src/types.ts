export type Items = {
  id: number;
  name: string;
  content: string;
  topic: string;
  category: string;
};

export type ItemsOmit = {
  name: string;
  content: string;
  topic: string;
  category: string;
}
